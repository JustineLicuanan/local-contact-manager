import { Box, Typography } from '@material-ui/core';

const Title = () => {
	return (
		<Box py={6}>
			<Typography variant='h4' align='center'>
				Contact Manager
			</Typography>
		</Box>
	);
};

export default Title;
